# --- Secrets & credentials (NUNCA al repo) ---
.env
.env.*
!.env.example
admin-credentials.txt
*.bak
chatwoot-secrets.txt

# --- Chatwoot patches binario grande (descargar en build) ---
docker/patches/pnpm

# --- Chatwoot runtime/build artifacts ---
node_modules/
.pnp.*
.yarn/cache
.yarn/install-state.gz

# Rails
log/*.log
log/*.gz
tmp/
coverage/

# Build artifacts (regenerables con assets:precompile)
public/assets/
public/packs/
public/vite/
public/sw.js.map

# IDEs / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp
*~

# WSL2 / Windows scratch
_*.bat
_*.log

# GitHub
.github/


# --- Added by Hermes on 2026-06-24 ---

# Large binary models (regenerable, no se versiona)
vendor/db/sentiment-analysis.onnx
vendor/db/*.onnx

# GitHub workflows (no aplican a este fork privado)
.github/workflows/
